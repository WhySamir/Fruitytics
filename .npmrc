# Security: Prevent installation of packages with known vulnerabilities
audit=true
audit-level=moderate

# Security: Use exact versions in production
save-exact=false

# Security: Prevent scripts from running during install
ignore-scripts=false

